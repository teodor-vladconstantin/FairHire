# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# typescript
*.tsbuildinfo

# LevelDB-backed private state + signing keys used by scripts/deploy-contract.ts
# (contains no seeds, but is machine-local and regenerable — the wallet seed
# itself lives in the separate wallet-setup project, never here)
/.dapp-state/

# LevelDB-backed private state written by the dev server's proof/attest
# routes — same nature as /.dapp-state/ above: machine-local, regenerable.
/midnight-level-db/

# claude code local session state
.claude/
